<div id="comment_edit_form">
  <%= form_with(model: [volunteer, comment]) do |form| %>
    <div class="input-field">
      <%= form.text_area :content, class: 'form-control', id: "comment_content_#{volunteer.id}" %>
      <%= form.submit '編集' %>
    </div>
  <% end %>
</div>
