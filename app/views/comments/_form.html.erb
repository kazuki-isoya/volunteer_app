<% if user_signed_in? %>
  <%= form_with(model: [volunteer, comment])  do |form| %>
    <div class="field">
      <%= form.text_area :content, class: 'form-control'  %>
    </div>
    <div class="actions">
      <%= form.submit 'コメントする' %>
    </div>
  <% end %>
<% end %>
