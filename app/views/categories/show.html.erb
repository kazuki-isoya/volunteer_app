<p>カテゴリー名：<%= @category.name %></p>
<p>
  画像:
  <%= image_tag @category.icon_url if @category.icon.present?  %>
</p>

<%= link_to '戻る', categories_path, class: 'btn btn-success btn-lg' %>
<%= link_to '編集', edit_category_path, class: 'btn btn-success btn-lg' %>
